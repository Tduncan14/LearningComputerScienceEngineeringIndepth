
//a built in javascript function called parseInt();
// turns a string into a number.
// but if the number cannot be converted into a string. The function will return nan(not a number);

function convertToInteger(str) {
  
    return parseInt(str);
  }
  
  convertToInteger("56");

  // will return the number 56