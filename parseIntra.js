// radix is the second argrument to the parseInt function. That works on the binary numbers from 0 to 10 as our base

function convertToInteger(str) {

return parseInt(str,2)
}

convertToInteger("10011");

console.log(convertToInteger("10011"));